import React from 'react'

export default function LinkedinLoginCallback() {
    React.useEffect(() => {
        //check if code and state exists
        //hit backend to finally login.

        // return () => {
        //     cleanup
        // }
    }, [])

    return (
        <div>
            Logging in with LinkedIn, wait ....
        </div>
    )
}
